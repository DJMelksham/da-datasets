(defun field-output (char-array
		     &optional
		       (file-missing-value NIL)
		       (data-missing-value NIL)
		       (use-global-string-pool? NIL))
  ()

  )

(defun field-string-p (char-array)

  )

(defun field-num-p (char-array)

  )

(defun field

(defun field-string (char-array
		     &optional
		       (file-missing-value NIL)
		       (data-missing-value NIL)
		       (use-global-string-pool? NIL)
					    
		     
		     
